name: Build Android Development

on:
  push:
    branches: ['main']

jobs:
  build_android:
    name: Build Android development client
    type: build
    params:
      platform: android
      profile: development
